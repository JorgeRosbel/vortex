---
import { cls } from '@/src/lib/utils';
interface Props {
  class?: string;
}
const { class: className } = Astro.props as Props;
---

<span class={cls('line-clamp-1', className)}>
  <slot />
</span>
