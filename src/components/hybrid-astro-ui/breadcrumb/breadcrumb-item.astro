---
import { cls } from '@/src/lib/utils';

interface Props {
  class?: string;
}
const { class: className } = Astro.props as Props;
---

<div
  data-slot="breadcrumb-item"
  class={cls('inline-flex items-center max-w-40 md:max-w-sm', className)}
>
  <slot />
</div>

