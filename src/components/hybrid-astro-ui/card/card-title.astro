---
import { cls } from '@/src/lib/utils';

interface Props {
  class?: string;
}

const { class: className } = Astro.props as Props;
---

<h2 class={cls('text-foreground font-semibold text-lg', className)}>
  <slot />
</h2>
