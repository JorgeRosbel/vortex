---
import { cls } from '@/src/lib/utils';

interface Props {
  class?: string;
}

const { class: className } = Astro.props as Props;
---

<div class={cls('text-foreground', className)}>
  <slot />
</div>
